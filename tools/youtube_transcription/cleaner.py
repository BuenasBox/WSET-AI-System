"""Transcript cleaning placeholders.

Future implementation will normalize transcripts conservatively while
preserving wine terminology, SAT vocabulary, causal explanations, examples,
and exam tips.
"""


def clean_transcript(raw_text: str) -> str:
    """Return a conservatively cleaned transcript.

    TODO: Normalize spacing and obvious caption artifacts without summarizing.
    """
    return raw_text
